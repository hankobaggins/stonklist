export function MockBanner() {
  return (
    <div className="mt-4 rounded-full border border-gold/40 bg-gold/10 text-gold text-[12px] font-semibold px-4 h-9 inline-flex items-center gap-2 w-full justify-center">
      demo mode — real StonkFun tokens, invented balances. add Supabase env to go live.
    </div>
  );
}
